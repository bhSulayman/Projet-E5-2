<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <link rel="stylesheet" href="css/styles.css">
</head>
<body>
    <header>
        <div class="nav-container">
            <div class="site-name">
                <h1>SneakVerse</h1>
            </div>
            <nav>
                <ul class="nav-links">
                    <li><a href="index.php?page=accueil">Accueil</a></li>
                    <li><a class="active" href="index.php?page=produit">Produit</a></li>
                    {# <li><a href="index.php?page=femme">Femmes</a></li> #}
                    {# <li><a href="index.php?page=compte">Compte</a></li> #}
                    <li><a href="index.php?page=panier"><i class="fas fa-shopping-cart"></i>Panier</a></li>
                    {%if session.login is defined %}
                    <li><a href="index.php?page=deconnexion">Se déconnecter</a></li>
                    {% else %}
                    <li><a href="index.php?page=connexion">Se connecter</a></li>
                    {% endif %}
                    {%if session.login is defined %}
                    <li><a href="#">{{session.login}}</a></li>
                    {% endif %}
                </ul>
            </nav>
        </div>
    </header>

{%if session.login is defined %}
    {%if session.role == 1 %}
    <li class="nav-item dropdown">
    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
    Administration
    </a>
    <div class="dropdown-menu" aria-labelledby="navbarDropdown">
    <a class="dropdown-item" href="#">Produit</a>
    <div class="dropdown-divider"></div>
    <a class="dropdown-item" href="#">Type</a>
    <div class="dropdown-divider"></div>
    <a class="dropdown-item" href="#">Utilisateur</a>
    </div>
    </li>
    {% endif %}
{% endif %}

    <main>
        {% block content %}
        
        {% endblock %}
    </main>

    <footer>
        <p>&copy; 2024 SneakVerse. Tous droits réservés.</p>
    </footer>
</body>
</html>
